---
const { text, desc } = Astro.props;
---

<div class:list="mt-16 md:mt-0">
  <h1 class="text-4xl lg:text-5xl font-serif font-bold lg:tracking-tight">
    <slot name="title">{text}</slot>
  </h1>
  {desc && <p class="text-lg mt-4 text-slate-600">
    <slot name="desc">{desc}</slot>
  </p>}
</div>
